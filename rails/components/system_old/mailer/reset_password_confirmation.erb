Your request to reset password is accepted.
Confirm your request information as below::

-- Accounnt Information --
         Login Name : <%= @user.login_name %>
  Registration Date : <%= @user.request_at.to_time %>
--

If this information is currect, click the following url and set your new password.
<%= @url %>
(if not linked, copy the url and paste it to your browser address bar.)
