include_partial 'setup'

open "/"
wait
assert_path "/pages/top"

click_link "Login"
wait

click_link "Reset Password"
wait
assert_path "/components/system/login/reset_password"

type 'login_name', 'yssk22'
type 'email', "yssk22@example.com"

click_button 'Reset'

set_timeout 5000

# TODO test code to confirm registeration
