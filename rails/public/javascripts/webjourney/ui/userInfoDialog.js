WebJourney.UI.UserInfoDialog = function(){
  this.initialize.apply(this, arguments);
};

WebJourney.UI.UserInfoDialog.prototype = jQuery.extend(
{
  initialize : function(login_name, open_trigger_link, callbacks){

  },

  build : function(o){

  }
});