# utility methods
def click_link(label)
  click "link=#{label}"
end

def click_button(label)
  click "//input[@value='#{label}']"
end

def assert_path(path)
  assert_location File.join("http://localhost:3000", path)
end

def wait_load(seconds = 5)
  wait_for_page_to_load seconds * 1000
end
alias :wait :wait_load

setup :fixtures => :all
open '/components/system/login/logout'
assert_title "Login - WebJourney"
